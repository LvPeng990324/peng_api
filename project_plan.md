你在帮我从零开发一个自用的 LLM API 分发网关，目标是替代 new-api 中我实际用到的那 10% 功能。

技术栈：Go 1.22+，单二进制交付；SQLite 单文件存储（用纯 Go 驱动 modernc.org/sqlite，不用 CGO）；路由用 chi 或标准库。

硬性约束：
1. 只做这些功能：统一 /v1/chat/completions 入口、Bearer token 鉴权、渠道管理（上游地址/key/模型映射/优先级）、失败重试与渠道自动禁用、完整请求/响应日志（成功失败都记）、两个 Web 页面（渠道管理、日志查看）。
2. 明确不做：多用户体系、注册登录、额度计费、兑换码、支付、绘图、审计、通知、Redis、MySQL。
3. 每个阶段先输出设计方案（表结构、接口定义、流程图文字描述），我确认后再写代码。
4. 关键路径（转发、SSE、重试）写单元测试；请求路径禁止 panic。
5. 交付物：可 go build 的完整代码 + 简短 README。

期待效果：
1. 拥有new-api那样api分发平台的基础功能，接入多个渠道多种模型，然后统一进行分发，渠道有优先级配置，失败后重试以及重试更低优先级渠道，以保证服务可用性
2. 能看到请求的具体内容，便于我进行agent调试
3. 有便捷的模型聚合配置，比如不同渠道对同一个模型命名不同，但实际上是同一个模型（比如glm-5.3、GLM-5.3、glm5.3，实际上都是同一个模型glm-5.3）我期望能比较方便的在一个集中的配置页配置模型别名功能，然后可以命中这些不同命名的模型，接着按照命中渠道的优先级进行请求分发